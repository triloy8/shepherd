# Web API is enabled only when SHEPHERD_SURFACES includes web in common.env.
# Generate a secret: bun -e 'console.log(require("node:crypto").randomBytes(32).toString("hex"))'
# Put the result here; do not commit web.env or put the token in a URL.
SHEPHERD_WEB_TOKEN=
# Listener is always 127.0.0.1. Use private Tailscale Serve for remote access.
SHEPHERD_WEB_PORT=8788
# Exact browser origins, comma-separated. Blank permits clients with no Origin only.
# Use the future UI origin here (HTTPS, or HTTP on loopback for development).
SHEPHERD_WEB_ORIGINS=
